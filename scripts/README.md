# scripts

Operational entry points for simulation and baseline utilities.

- run_simulation.py: deterministic staged grasp demo used for presentation.
- generate_dataset.py: legacy random-action dataset generation.
- train_model.py: legacy RandomForest baseline training.